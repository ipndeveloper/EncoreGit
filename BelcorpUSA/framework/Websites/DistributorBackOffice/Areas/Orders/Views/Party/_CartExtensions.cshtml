@* This is an example of wiring up event handlers to the CartModel. *@
@*<script type="text/javascript">
	$(function () {
		ko.utils.arrayForEach(cartModel.CustomerCarts(), function (customerCartModel) {
			customerCartModel.onProductSelected(function (event, productData) {
				alert(productData.productId);
			});
			customerCartModel.onCartItemAdding(function (event, addToCartData) {
				alert(addToCartData.productId);
				
				// You can cancel the "AddToCart" call thusly:
				event.preventDefault();
			});
		});
	});
</script>*@
